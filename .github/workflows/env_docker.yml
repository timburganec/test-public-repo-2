name: deploy

on: push

jobs:
  deployTerraform:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: ls -lh
    - uses: ./.github/actions/dockered
      env:
        MEH: ${{secrets.MEH}}

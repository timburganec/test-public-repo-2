# dev_deploy.yml
on: workflow_dispatch
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
    - run: echo "S3_BUCKET=dev" >> $GITHUB_ENV
    - run: echo $S3_BUCKET
    - run: echo "${{ github.sha }}"
